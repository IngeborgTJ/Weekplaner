<h1> My to do list:</h1>

<h3>Old Plans: "August - November 2014 - Forbredelser til ny jobb - Egenutvikling" - Completed</h3>

<table>
  <thead>
    <tr>
      <th> </th>
      <th></th>
      <th></th>
    </tr>
  </thead>

<h2> New plans:</h2>
  
  <tbody>
    <% @weekplans.each do |weekplan| %>
      <tr>
        <td><%= weekplan.plan %></td>
        <td><%= link_to 'Show', weekplan %></td>
        <td><%= link_to 'Edit', edit_weekplan_path(weekplan) %></td>
        <td><%= link_to 'Delete', weekplan, method: :delete, data: { confirm: 'Are you sure you want to delete this plan?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New plan', new_weekplan_path %>
